/**
 * Engine code version, recorded on every persisted calculation
 * (database-design.md §6.1) so a historical result is exactly reproducible.
 * Distinct from the ruleset version (the tax-year "numbers").
 */
export const ENGINE_VERSION = "0.1.0"
